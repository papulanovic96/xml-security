spring:
  application:
    name: zuul-server
  cloud:
    config:
      failFast: true
server.port: 8761

management:
        endpoints:
                web:
                  exposure:
                    include: "*"

